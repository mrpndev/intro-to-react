import React from 'react'

export default function Greet() {
	let firstName = "Paul"

	return (
		// ? { } example of an expression
		<h3>Welcome, {firstName}</h3>
	)
}

/* 
	? create a component called Counter
	* counter should have two buttons: + and -
	* counter should also have a span
	* the span should hold the value of a variable called count
*/
